import { assert } from "chai";

import { MockInteraction } from "../../src/mocks/MockInteraction";

suite("this is an example", () => {
  test("using assert", () => {
    assert.isDefined(MockInteraction);
  });
});
