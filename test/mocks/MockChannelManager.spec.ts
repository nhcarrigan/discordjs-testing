import { assert } from "chai";

suite("Mock Channel Manager", () => {
  /**
   * Instantiation.
   */
  test("should instantiate", () => {
    assert.fail();
  });

  /**
   * Properties.
   */

  test("should have a cache property", () => {
    assert.fail();
  });

  /**
   * Methods.
   */

  test("should be able to fetch a channel", () => {
    assert.fail();
  });

  test("should be able to fetch all channels", () => {
    assert.fail();
  });
});
