import { assert } from "chai";

suite.skip("Mock User", () => {
  test("should instantiate", () => {
    assert.fail();
  });

  test("should get ID", () => {
    assert.fail();
  });

  test("should get username", () => {
    assert.fail();
  });

  test("should get tag", () => {
    assert.fail();
  });

  test("should get discriminator", () => {
    assert.fail();
  });

  test("should get avatar", () => {
    assert.fail();
  });

  test("should get whether the user is a bot", () => {
    assert.fail();
  });

  test("should get whether the user is a system user", () => {
    assert.fail();
  });

  test("should get list of sent messages", () => {
    assert.fail();
  });

  test("should fetch user", () => {
    assert.fail();
  });

  test("should display avatar URL", () => {
    assert.fail();
  });

  test("should send message", () => {
    assert.fail();
  });
});
