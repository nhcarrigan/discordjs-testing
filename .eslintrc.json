{
  "extends": "@nhcarrigan",
  "rules": {
    "require-await": "off"
  },
  "overrides": [
    {
      "files": "test/mocks/MockMessage.spec.ts",
      "rules": {
        "camelcase": ["error", { "allow": ["proxy_url"] }]
      }
    }
  ]
}
